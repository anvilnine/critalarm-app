/// The app version string, mirrored from `pubspec.yaml` `version:` and shown
/// on the Setup page footer.
///
/// Keep this in sync with the `version:` line in `pubspec.yaml`.
/// both must be updated together in the same commit.
const String appVersion = '0.1.0+1';
